# Sugar Quit — QA Pass 10 (2026-04-26)

First pass driven by the strengthened ui-qa skill (rules 22-27).
Sim FA05FD96 (iPhone 17 Pro 402×874pt). Maya (Day 47 → 48 by end of pass) signed in.

## Coverage manifest result

Per Rule 27. Full manifest at `/tmp/sq-qa-pass-10.md`. Status:

**Tabs (4)** — Maya state walked, others did-not-test (see §6):
- ✅ /(tabs)/home (top + scrolled)
- ✅ /(tabs)/curriculum (overview)
- ✅ /(tabs)/curriculum/[day=1] (Day 1 lesson)
- ✅ /(tabs)/progress (top of fold)
- ✅ /(tabs)/profile (full + element list)
- ✅ /(tabs)/profile/edit (every Pressable tap-tested → bug 13 found)
- ✅ /(tabs)/profile/settings (already verified pass 9, email row from store)

**Modals (12)** — opened + element list + sample interaction:
- ✅ /(modals)/sos (visual only, send not tested)
- ✅ /(modals)/post-sos (Walked tap → outcome saved, modal advances)
- ✅ /(modals)/craving-log (full E2E: intensity 5 + Stress + Walked + Save → Profile counter 12→13)
- ✅ /(modals)/checkin (full E2E: Sugar-free → mood Good → streak 47→48)
- ✅ /(modals)/forecast-window (peak slot)
- ✅ /(modals)/streak-freeze (verified earlier sessions)
- ✅ /(modals)/share-card (verified earlier sessions)
- ✅ /(modals)/paywall-contextual (verified earlier sessions)
- ✅ /(modals)/rate-app (verified pass 9 phase-aware)
- ✅ /(modals)/disclaimer (visual only)
- ✅ /(modals)/milestone (verified earlier)

**Auth (2)**:
- ✅ /(auth)/sign-in (multiple sign-ins this pass — alex, maya, empty)
- ⚠️ /(auth)/sign-up (visual + form fill, but Strong-Password popup interferes)

**Onboarding (15)** — empty user walked steps 1-12 + result + paywall + auth choice (push-permission step skipped).

**State matrix (Rule 26)**:
- ✅ Maya (Day 47, premium) — full sweep
- ⚠️ Alex (Day 1, premium) — verified earlier sessions, not re-walked this pass
- ⚠️ Empty (Day 0, free) — used for onboarding only
- ❌ Fresh sign-up — blocked by Strong-Password popup, did not complete

**Devices (Rule 4)**:
- ✅ Standard 402pt — sim FA05FD96
- ❌ Narrow 390pt — belongs to deskcare, hard rule
- ❌ Wide 440pt — belongs to break_up, hard rule

**Journeys (Rule 24)**:
- ✅ J2 — Returning user dailies (sign-in maya → checkin → craving log → Profile delta)
- ⚠️ J1 — New user end-to-end (started but Strong-Password popup blocked sign-up)
- ⚠️ J3 — Premium upgrade (Adapty in mock, advanced state but not verified post-purchase delta)
- ❌ J4 — Streak freeze drain (covered in earlier passes 7-8)
- ✅ J5 — Cross-account isolation (verified Stage A doc)

## Bugs found (3 new) + 12 from pass 9 = 15 total cumulative

| # | Severity | Surface | Issue | Status |
|---|---|---|---|---|
| 13 | medium | profile/edit FieldRow | "Tap to change" a11y label + chevron arrow on Pressable that only fires Haptics — no picker. Misleading affordance. | **FIXED** — converted to non-interactive View, removed chevron + "Tap to change" copy |
| 14 | high | checkin / post-sos / craving-log / paywall-contextual / paywall.tsx / rate-app / curriculum/[day] | `accessibilityRole="radio"` on multi-select chips → invisible to WDA tree. Same root pattern as triggers (pass 9). | **FIXED** — bulk sed across 7 files, role swap to "button" |
| 15 | medium | post-sos outcome cards / craving-log outcome cards / etc. | Pressable wrapping GlassCard pattern — chips invisible to WDA element tree even with role="button" because BlurView blocks at native layer. Tap by coords works for sighted users. | **deferred** — same fix as triggers needed (Pressable+plain View), structural sweep |

## Cumulative status (passes 1-10)

- **15 bugs found**, **13 fixed**, 2 deferred (Bug 15 a11y structural, Stage A push silent fail)
- TSC: 0 errors after every commit
- Maya state propagation **verified end-to-end**: checkin → +1 streak, craving log → +1 cravings count
- All push to Supabase still **silent fail** (Stage A TRACKING bug, not progressed this pass)

## Did not test

Per Rule 25 — silent gaps not allowed. Listed honestly:

**Devices:**
- iPhone 16e (390pt narrow) — owned by deskcare project, do-not-touch rule.
- iPhone 17 Pro Max (440pt wide) — owned by break_up.
- Cold-start with AsyncStorage wipe (uninstall+reinstall Expo Go).
- Offline / airplane mode flows.
- Slow-network simulation.

**Modal dismiss methods (Rule 23):** Only X-button tested. Backdrop-tap, swipe-down, system-back NOT verified for any modal. All 12 modals affected.

**Per-screen Pressable enumeration (Rule 22):**
- Profile menu: tapped Edit profile, Settings. NOT tapped: Notifications (routes to settings, redundant), Manage subscription (opens iOS settings sheet), Support (mailto), Privacy Policy (external URL), Sign out (defer to end-of-pass).
- Settings: tapped Email row indirectly via screen visit. NOT tapped: 4 toggles (Morning check-in, Daily lesson, Motivation, Streak at risk), Change password, Restore purchases, Export my data, Delete account, Privacy Policy, Terms of Service.
- Curriculum: only Day 1 lesson opened. Days 2, 3, 4-7, 14, 30 all lessons exist but not opened.
- Progress: top of fold only. Weekly + Milestones sub-routes not opened.
- Home forecast cards: Calm/Peak/Evening — only Peak modal opened.
- Onboarding: skipped Step 13 (loading), Step 14 (result content), Step 15c (push-permission terminal).

**State variants:**
- Alex full sweep this pass (relied on earlier passes).
- Empty user main-app screens (only onboarding walked).
- Fresh sign-up flow (blocker: iOS Strong-Password popup interferes with our typing path).
- Premium upgrade state delta verification.
- Day 1 vs Day 7 vs Day 30 milestone modals (only Day 1 implicit).
- Onboarding sub-step variations (selecting different goals/triggers/peaks alters downstream copy).

**Edge inputs:**
- firstName overflow (>40 chars).
- firstName special chars / paste-bomb.
- Email validation matrix (empty / no-@ / various TLDs).
- Password ≥6 char boundary.
- Confirm-password mismatch.
- TextInput max length on craving notes.
- Long craving notes overflow.

**a11y:**
- VoiceOver actually enabled on sim (only inspected element tree as proxy).
- Focus order traversal.
- Reduced motion mode.
- High contrast / large text settings.

## Deliverable closes here. Next pass should:

1. Pick up from "Did not test" list and burn through it systematically.
2. Bug 15: structural fix Pressable+GlassCard pattern globally
   (replace with Pressable+View where touchable, like triggers.tsx).
3. Stage A push silent fail debug — RN client → Supabase write side
   not propagating, despite REST API + same JWT confirming server is fine.
