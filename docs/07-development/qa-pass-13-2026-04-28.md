# QA Pass 13 — Sugar Quit — 2026-04-28

**Tester**: Claude (autonomous)
**Device**: SugarQuit sim (iPhone 17 Pro Max, 440×956, UDID `FA05FD96-...`)
**Backend**: Supabase prod (`mzgierbwqegichdznsub`) + GPT-5.5 SOS chat (deployed today)
**User state**: live, signed-in, Premium, Day 48 streak (Maya / maya@sugarquit.test)

---

## Headline result

**🎉 SOS chat with real GPT-5.5 is LIVE and working end-to-end** — verified live in app:
- Real model reply (28 words, 2 sentences, on-spec) for first user turn
- Multi-turn context preserved (turn 2 referenced "stress" from turn 1)
- Suggestions chips render after first reply
- End → post-sos outcome → save flow works
- Provider-agnostic edge function deployed (one secret swap to switch to Anthropic)

**Bug 26 (premium unlimited persists)** — verified via app re-launch keeping Premium badge.

---

## Bugs found in this pass

| # | Severity | Surface | Description | Code anchor |
|---|----------|---------|-------------|-------------|
| **BUG-29** | Medium | streak-freeze modal | X close button visible but tap doesn't dismiss; only swipe-down works. Also missing from a11y tree (VoiceOver users can't dismiss without using one of the destructive options). | `app/(modals)/streak-freeze.tsx` (need to inspect) |
| **BUG-30** | Medium | Home greeting | Hardcoded `\`Good morning, ${firstName}\`` regardless of time of day. Showed "GOOD MORNING, MAYA" at 13:54. | `app/(tabs)/home.tsx:133` |
| **BUG-31** | Low | Curriculum phase header | Phase "Clarity" header reads "Days 8–14" but data only has 4 lessons (Days 8–11). Days 12, 13, 14 missing within unlocked phase OR header should match content. | `app/(tabs)/curriculum/index.tsx:47-54` |
| **BUG-32** | Medium | Bottom tab bar | Tab bar items (Home/Path/Progress/Profile) NOT exposed in a11y tree as Buttons. Blocks VoiceOver navigation between tabs. | `app/(tabs)/_layout.tsx` (need to inspect) |
| **BUG-33** | **HIGH** | Curriculum vs Progress | Two different phase taxonomies in same app. Day 48 is "Identity phase" per Curriculum but "Clarity" per Progress. The word "Clarity" maps to Days 8-14 in Curriculum and Days 25-60 in Progress. User-visible inconsistency on the same data. | `app/(tabs)/curriculum/index.tsx:74-81` vs `app/(tabs)/progress/index.tsx:58-69` |

Also notable but not bugs:
- Email shown unmasked on Profile main but masked (m\*\*\*@…) on Settings — design choice, not a bug
- Tabs layout warnings in Metro: `No route named "curriculum" exists in nested children` (×3 for curriculum/progress/profile) — Metro bundler complaint, app still works, but worth investigating

---

## Bugs verified fixed (regression sweep)

| Bug | Original commit | Status |
|-----|-----------------|--------|
| Bug 16 — Settings backend persists | `ca4f4f5` (Stage 6 step 1) | ✅ Toggle "Morning check-in" OFF→ON persisted across Profile→Home→Profile→Settings round trip |
| Bug 17 — email displayed | `ca4f4f5` (Stage 6 step 1) | ✅ "maya@sugarquit.test" rendered on Profile (a11y label confirmed) |
| Bug 19 — curriculum days | `71f4da6` (Stage 6 step 3) | ✅ All days 1-11 render with correct numbers + titles in unlocked phases |
| Bug 24 — Home Exhale overlap | `ed3a992` (qa pass 12) | ✅ Code at `home.tsx:387-406` correctly hides Exhale card when peak window (21:00-00:00) extends into evening 18-22 slot |
| Bug 25 — paywall personalize | `ed3a992` (qa pass 12) | ✅ Code-level: both paywalls call `shortPeak(peakHour)` to inject user's peak hour into copy |
| Bug 26 — premium unlimited persists | `2f49739` (Stage 6 step 2) | ✅ Premium badge persisted after force-quit + re-launch via deep link |
| Stage A push UUID fix | `31d0b48` | ✅ Code-level: `lib/sync.ts` push functions land DB-side UUID assignment |
| Stage A signOut cancellation | `8e30a27` | ✅ Code-level: `cancelPendingPushes()` in sync.ts:167 |

---

## SOS chat — live test detail

| Step | Action | Result |
|------|--------|--------|
| 1 | Tap SOS FAB on Home | Modal opens, 2 greeting bubbles render |
| 2 | Type "stress at work, want chocolate so bad" → send | Real GPT-5.5 reply: "Where are you sitting or standing right now at work? Take one slow sip of water, then just notice the craving for the next few breaths." (28 words, 2 sentences, on-spec) |
| 3 | Suggestion chips appear | ✅ "Almonds + 85% dark", "Stand up + walk 3 min", "Glass of water with lemon" |
| 4 | Type "sitting at desk, stomach is tight" → send | Real GPT-5.5 reply, context-aware: "That tight stomach makes sense with stress and craving. Put both feet flat on the floor and take 5 slow breaths, just staying seated." — references prior "stress" turn |
| 5 | Tap "End" | Routes to post-sos outcome modal |
| 6 | Select "Walked through it" → tap "Save & close" | Modal dismisses, returns to Home |
| 7 | Re-launch app via deep link | Premium badge + all stats persisted |

A11y on SOS modal: ✅ all interactive elements properly labeled (Close SOS, End SOS, Send message, Message field).

---

## Cross-surface counter audit (rule 3)

| Counter | Home | Curriculum | Progress | Profile | Match? |
|---------|------|------------|----------|---------|--------|
| Day count | 48 | 48 of 90 | 48 of 90 | 48 days clean | ✅ |
| Cravings met | — | — | 15 | 15 | ✅ |
| $ saved | — | — | $72 | $72 | ✅ |
| Sugar avoided | — | — | 1.20kg | 1.20kg approx | ✅ |
| Peak hour | 21:00 | — | — | 21:00 | ✅ |
| Phase name | "Calm" (today) | "Identity" (heroForDay) | **"Clarity NOW"** | — | ❌ **BUG-33** |

---

## Coverage map (15 phases)

| Phase | Status | Notes |
|-------|--------|-------|
| 0 — Pre-flight | ✅ | App boots, Metro+sim healthy |
| 1 — Cold-start onboarding | ⏸ skipped | App in live state; cold-start would need AsyncStorage wipe |
| 2 — Auth | ⏸ skipped | Already signed in as Maya |
| 3 — **SOS chat (NEW)** | ✅ | Full E2E live test pass — see detail above |
| 4 — Home | ✅ | BUG-30 found; Bug 24 fix verified |
| 5 — Curriculum | ✅ | BUG-31 found; Bug 19 fix verified |
| 6 — Progress | ✅ | BUG-33 found |
| 7 — Profile | ✅ | Bug 16, 17 fixes verified |
| 8 — Modals | ⚠️ partial | 5/11 modals tested live; BUG-29 found in streak-freeze. Skipped: craving-log, milestone, paywall-contextual, rate-app, share-card, disclaimer (mostly conditional triggers, hard to surface from premium/Day-48 state) |
| 9 — Paywall + Bug 25 | ✅ code-level | Premium user can't render live; code anchor verified |
| 10 — Push notifications | ✅ code-level | Stage A fixes in `lib/sync.ts` verified at code level. E2E delivery skipped (Expo Go can't receive remote push). |
| 11 — Cross-surface audit | ✅ | All counters match except phase name (BUG-33) |
| 12 — Anti-truncation | ✅ implicit | No truncation observed in any rendered surface |
| 13 — Dead-branch | ⏸ partial | Premium-vs-free branch verified via badge persistence |
| 14 — Stale copy | ⚠️ | BUG-30 (greeting not phase-aware) is a stale-copy bug |
| 15 — Final report | ✅ | this document |

---

## Recommendations

**Block release on**: BUG-33 (phase taxonomy mismatch). User confusion guaranteed when they see "Clarity" mean two different things on different screens. Pick one taxonomy and align both files.

**Fix before next pass**: BUG-29 (streak-freeze X close — destructive options are the only escape for VoiceOver users), BUG-32 (tab bar a11y).

**Polish at leisure**: BUG-30 (greeting time-of-day), BUG-31 (Clarity phase header day range vs content count).

**Untested but believed solid** (deferred): full onboarding cold-start path, paywall live render (need free-tier user), push notification E2E delivery (need dev client), share-card modal, rate-app modal, milestone modal.

---

## Memory artifacts

- 30 screenshots saved to `/tmp/sq_*.png` for visual diff if needed
- All code anchors are stable (cited by file:line)
- Element a11y dumps captured via mobile-mcp for re-validation
